FROM node:20-alpine

WORKDIR /app

COPY sovereign-site/identity-record-storage-server.js ./identity-record-storage-server.js
COPY containerization/preinit-demo-identities.js ./preinit-demo-identities.js

ENV PORT=4000
ENV POD_NAME=holder-pod
ENV BUSINESS_NAME=business

EXPOSE 4000

CMD ["sh", "-c", "node preinit-demo-identities.js && node identity-record-storage-server.js"]
